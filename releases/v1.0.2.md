## 설치 앱 실행 경로

- 일반 사용자용 `run.bat`과 `run-desktop.bat`은 설치된 시작 메뉴 앱만 실행합니다.
- Node.js, npm, Rust, Cargo와 소스 빌드는 일반 실행에 필요하지 않습니다.

## 개발 경로 분리

- 소스 개발은 `run-dev.bat` 또는 `run-web.bat`을 사용합니다.
- 개발용 Cargo target은 사용자 데이터와 분리된 `%LOCALAPPDATA%\\WrongAnswerNotebookDev\\cargo-target`에 둡니다.
